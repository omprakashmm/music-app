# ──────────────────────────────────────────────────────────────
# SonicStream Environment Variables
# For LOCAL dev: copy this file to .env and fill in values
# For RAILWAY:   set these in the Railway dashboard → Variables tab
# ──────────────────────────────────────────────────────────────

# Spotify API credentials (for Spotify playlist import)
# Create an app at: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Database path
# - Local dev: leave blank (defaults to music.db in project root)
# - Railway:   set to /data/music.db and mount a Volume at /data in the dashboard
DB_PATH=/data/music.db

# Port — Railway assigns this automatically. Do NOT override on Railway.
# PORT=3000
